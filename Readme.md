## C++ OpenGL Bulletphysics test project.

# Build instructions
build: `mkdir build && cd build && cmake ../ && make`

run: `cd bin && ./engine`

# Instructions
WASD to move and ALT to capture and release mouse.